// Full command registry + scrollback implemented in the terminal-app milestone.
export function TerminalWindow() {
  return <div className="app-placeholder">terminal window — content coming soon.</div>
}
